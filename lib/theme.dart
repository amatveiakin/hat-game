import 'package:flutter/material.dart';

class MyColors {
  static const primary = Color(0xff4b0082);
  static const primaryDark = Color(0xff310062);
  static const accent = Color(0xffccad00);
  static const accentDark = Color(0xff998000);
}
